import React from 'react';
import { Link } from 'react-router-dom';

const BtnLogin = () => {
    return (
        <div>

            <Link to="/login">

                <button>LOG IN</button>

            </Link> 
        
        </div>
    )
}

export default BtnLogin
